resources:
  pipelines:
    dbx_bronze_rt:
      name: DBX-RT-Bronze
      serverless: true
      continuous: true          # always-on — picks up new files as they arrive
      catalog: ${var.catalog}
      schema: ${var.schema}
      libraries:
        # Real-time + near-RT bronze streaming tables:
        #   bronze_digital_events  (real-time ~10s)
        #   bronze_credit_bureau   (near-RT micro-batch ~60s)
        #   bronze_telco_events    (near-RT micro-batch ~45s)
        - notebook:
            path: /Workspace/Users/marcus.tay@databricks.com/rfp-presentation-sep26/files/part_a_customer_360/10_sdp_bronze_rt
      channel: CURRENT
      development: true
